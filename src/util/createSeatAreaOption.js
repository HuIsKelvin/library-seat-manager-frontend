/**
 * 
 * @param {Object} data 
 */
function processData(data) {
  let seriesData = [];

  return seriesData;
}

/**
 * function - createSeatAreaOption
 * 基于 echarts.js 的散点图，创建座位区域
 * @param {Object} data
 * @returns {Object} option
 */
export function createSeatAreaOption(data) {
  let option = {};
}